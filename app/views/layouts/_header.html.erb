<!-- headerリスト -->
<header>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand text-white" href="/"><span>鉄分</span></a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavDropdown">
         <ul class="navbar-nav ml-auto">
          <% if user_signed_in? %>
            <li>
              <%= link_to 'MyPege', edit_user_path, method: :get %>
            </li>
            <li>
              <%= link_to 'ログアウト', destroy_user_session_path, method: :delete %>
            </li>
          <% else %>
            <li>
              <%= link_to '新規登録', new_user_registration_path, method: :get, class: 'fas fa-user-plus nav-link text-white' %>
            </li>
            <li>
              <%= link_to 'ログイン', new_user_session_path, method: :get, class: 'fas fa-sign-in-alt nav-link text-white' %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>
</header>