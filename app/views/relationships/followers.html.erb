<!-- フォロワー一覧画面 -->
<div class="container">
  <div class="row">
    <div class="col-sm-12 mx-auto">
      <h1 class="heading">フォロワー一覧</h1>
        <% if @users.empty? %>
          <p>ユーザはいません</p>
        <% else %>
          <%= render 'users/index', users: @users %>
        <% end %>
    </div>
  </div>
</div>