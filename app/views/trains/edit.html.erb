<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
  		<h1>投稿編集画面</h1>
    	<%= render 'layouts/errors', obj: @train %>

  		<!-- 投稿内容編集フォーム -->
  		<%= render 'trains/edit_form', train: @train %>

  		<!-- 各リンクを表示 -->
  		<%= link_to '詳細ページへ', train_path(@train), class: "btn btn-primary" %>　|　<%= link_to 'Topへ', root_path, class: "btn btn-secondary" %>
  </div>
</div>